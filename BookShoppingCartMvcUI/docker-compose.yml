version: '3.8'

services:
  db:
    image: mcr.microsoft.com/mssql/server:2019-latest
    container_name: ptmsql
    environment:
      - ACCEPT_EULA=Y
      - MSSQL_SA_PASSWORD=Ptm@19941225
    volumes:
      - bookstore:/var/opt/mssql
    ports:
      - "1434:1433"
    networks:
      - bookstore-net
    healthcheck:
      test: ["CMD-SHELL", "/opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P Ptm@19941225 -Q 'SELECT 1' || exit 1"]
      interval: 10s
      retries: 5
      start_period: 15s
      timeout: 5s
    restart: unless-stopped

  migration:
    build:
      context: .
      dockerfile: Dockerfile.withoutbundle
    container_name: bookstore_Migration
    environment:
      ConnectionStrings__DefaultConnection: "Server=db;Database=BookStoreDb;User Id=sa;Password=Ptm@19941225;TrustServerCertificate=True"
    networks:
      - bookstore-net
    depends_on:
      db:
        condition: service_healthy
    restart: "no"  # usually migrations run once, no restart

  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bookstore
    ports:
      - "8080:8080"
    environment:
      ConnectionStrings__DefaultConnection: "Server=db;Database=BookStoreDb;User Id=sa;Password=Ptm@19941225;TrustServerCertificate=True"
    networks:
      - bookstore-net
    depends_on:
      migration:
        condition: service_completed_successfully
    restart: unless-stopped

volumes:
  bookstore:
networks:
  bookstore-net:
    driver: bridge